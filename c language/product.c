#include <stdio.h>
int main(){
    int a,b,product;
    printf("Enter the two no. a and b :");
    scanf("%d %d",&a,&b);
    product=a*b;
    printf("The product of two no. is : %d",product);
    return 0;
}